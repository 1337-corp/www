// Twitter shares reuse the Open Graph card so the "summary_large_image" card
// declared in layout metadata always resolves to a real image.
export { default, alt, size, contentType } from "./opengraph-image";
